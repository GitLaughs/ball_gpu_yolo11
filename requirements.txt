# ============================================================
# Basketball Shot Detection System
# requirements.txt - 正常 UTF-8 格式
# 安装命令: pip install -r requirements.txt
# ============================================================

# ---------- Web 框架 ----------
Flask==3.1.0
Flask-Cors==4.0.0
Werkzeug==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.0

# ---------- 计算机视觉 ----------
# 注意：opencv-contrib 包含 opencv-python，二选一即可
opencv-contrib-python==4.10.0.84
opencv-python==4.12.0.88
Pillow==11.1.0

# ---------- 数值计算 ----------
numpy==1.26.4
scipy==1.16.3
matplotlib==3.9.2

# ---------- PyTorch (CUDA 12.1) ----------
# 如果您没有 NVIDIA GPU，请改为:
#   torch==2.5.1
#   torchvision==0.20.1
#   torchaudio==2.5.1
# CUDA 版本通过以下方式安装（install_gpu.bat 已自动处理）:
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# ---------- YOLO / 目标检测 ----------
ultralytics==8.3.227

# ---------- 工具库 ----------
tqdm==4.67.1
psutil==7.1.3
requests==2.32.5
PyYAML==6.0.3
colorama==0.4.6
filelock==3.20.0
certifi==2025.11.12
charset-normalizer==3.4.4
idna==3.11
urllib3==2.5.0
packaging==25.0
typing_extensions==4.15.0
six==1.17.0
python-dateutil==2.9.0.post0
setuptools==80.9.0
wheel==0.45.1

# ---------- 可选（GPU 加速，需要 NVIDIA GPU） ----------
cupy-cuda13x==13.6.0

# ---------- 可选（标注工具，开发时用） ----------
# labelImg==1.8.6
